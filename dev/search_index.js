var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"","title":"API Reference"},{"category":"section","location":"api/#Types","page":"API Reference","text":"","title":"Types"},{"category":"section","location":"api/#Functions","page":"API Reference","text":"","title":"Functions"},{"category":"type","location":"api/#Martini.Mesher","page":"API Reference","text":"Mesher(grid_size::Integer = 257)\n\nPrecompute the implicit RTIN binary-tree triangle coordinates for a tile of size grid_size × grid_size. grid_size must be 2^k + 1 for some k >= 1 (i.e. 3, 5, 9, 17, 33, 65, 129, 257, 513, …). Constant per grid size — reuse a single Mesher across many terrains via create_tile.\n\n\n\n\n\n","title":"Martini.Mesher"},{"category":"type","location":"api/#Martini.MesherCache","page":"API Reference","text":"MesherCache(grid_size::Integer)\nMesherCache(mesher::Mesher)\n\nPreallocated scratch buffer for get_mesh. Holds the per-call Matrix{UInt32} index map (1 MB at grid_size = 513). Pass via the cache kwarg to avoid the per-call allocation in hot loops:\n\ncache = MesherCache(mesher)\nfor err in 1:50\n    mesh = get_mesh(tile; max_error = err, cache)\nend\n\nOne MesherCache per concurrent task — it is not thread-safe to share. It deliberately does not live on the Mesher itself so that a single Mesher remains safely shareable across threads.\n\n\n\n\n\n","title":"Martini.MesherCache"},{"category":"type","location":"api/#Martini.Tile","page":"API Reference","text":"Tile{T<:AbstractFloat}\n\nA heightfield bound to a Mesher, together with the per-vertex maximum approximation error map. Constructed via create_tile; pass to get_mesh to extract a triangle mesh at any error threshold.\n\nT is the storage eltype, inferred from the input array passed to create_tile (defaults to Float32 for non-AbstractFloat input).\n\nFields:\n\nmesher::Mesher — the precomputed RTIN structure\nterrain::Matrix{T} — (grid_size, grid_size), indexed terrain[x, y] (1-based)\nerrors::Matrix{T}  — same shape; errors[x, y] is the worst error observed at grid position (x, y) across all triangle levels.\n\n\n\n\n\n","title":"Martini.Tile"},{"category":"type","location":"api/#Martini.Mesh","page":"API Reference","text":"Mesh{P,F}\n\nResult of get_mesh. vertices::Vector{P} and triangles::Vector{F}, both 1-based by default.\n\nP is constructed as P(x, y) where (x, y) are 1-based grid coordinates in [1, grid_size].\nF is constructed as F(a, b, c) where (a, b, c) are 1-based column indices into vertices.\n\nDefaults are P = NTuple{2,UInt16} and F = NTuple{3,UInt32} so there is no runtime dependency on GeometryBasics. For GL-ready output:\n\nusing GeometryBasics\nmesh = get_mesh(tile; point_type = Point2{UInt16}, face_type = GLTriangleFace)\n\nGLTriangleFace stores its indices in OffsetInteger{-1, UInt32}, so 1-based inputs become 0-based on construction — directly GL-uploadable.\n\n\n\n\n\n","title":"Martini.Mesh"},{"category":"function","location":"api/#Martini.create_tile","page":"API Reference","text":"create_tile(mesher::Mesher, terrain) -> Tile\n\nBuild a Tile for the given terrain heightfield. terrain may be a Vector of length grid_size^2 (y-major, matching the Mapbox/JS convention) or a Matrix of size (grid_size, grid_size) indexed terrain[x, y] with 1-based (x, y). The two layouts share memory after reshape. Computes the per-vertex max-error map eagerly.\n\nThe element type of terrain determines the Tile{T} eltype: pass Matrix{Float64} to get a Tile{Float64}. Non-AbstractFloat input is collected into Float32.\n\n\n\n\n\n","title":"Martini.create_tile"},{"category":"function","location":"api/#Martini.get_mesh","page":"API Reference","text":"get_mesh(tile::Tile; max_error = 0,\n         point_type = NTuple{2,UInt16},\n         face_type  = NTuple{3,UInt32},\n         cache      = MesherCache(tile.mesher)) -> Mesh\n\nWalk the implicit RTIN binary tree top-down, emitting a triangle whenever the error at its long-edge midpoint is at or below max_error. Returns a Mesh{P,F} where vertices are constructed point_type(x, y) and triangles are constructed face_type(a, b, c). All indices are 1-based.\n\nPass an explicit cache::MesherCache in hot loops to avoid reallocating the internal index buffer on every call.\n\n\n\n\n\n","title":"Martini.get_mesh"},{"category":"section","location":"#Martini.jl","page":"Home","text":"A Julia port of Mapbox Martini: real-time Right-Triangulated Irregular Networks for terrain mesh generation. Given a (2^k + 1) × (2^k + 1) heightfield, it generates a hierarchy of triangle meshes at any error threshold in milliseconds.\n\nBased on \"Right-Triangulated Irregular Networks\" by Will Evans et al. (1997).","title":"Martini.jl"},{"category":"section","location":"#Install","page":"Home","text":"import Pkg\nPkg.add(url = \"https://github.com/JuliaGeo/Martini.jl\")","title":"Install"},{"category":"section","location":"#Quick-start","page":"Home","text":"using Martini\n\nm = Mesher(257)                                # 2^8 + 1 grid\ntile   = create_tile(m, terrain)               # length-65 025 Vector{Float32}, y-major\nmesh   = get_mesh(tile; max_error = 10)        # 10-metre approximation error\n\nOutput:\n\nmesh.vertices  :: Vector{Tuple{UInt16, UInt16}} — each element is a 1-based (x, y) grid coordinate in [1, grid_size].\nmesh.triangles :: Vector{Tuple{UInt32, UInt32, UInt32}} — each element is a triple of 1-based indices into mesh.vertices.\n\nFor WebGL/OpenGL, subtract 1 from both vertices and triangles — or use GeometryBasics' GLTriangleFace, which stores 0-based offsets natively:\n\nusing GeometryBasics\nmesh = get_mesh(tile;\n    max_error  = 10,\n    point_type = Point2{UInt16},\n    face_type  = GLTriangleFace,\n)","title":"Quick start"},{"category":"section","location":"#Type-parameters","page":"Home","text":"Tile{T<:AbstractFloat} is inferred from eltype(terrain): pass Matrix{Float64} to get a Tile{Float64}. Default is Float32.\nMesh{P,F} is controlled by the point_type and face_type kwargs. Both default to plain NTuples (no GeometryBasics runtime dep). Any callable that accepts T(x, y) / T(a, b, c) works, plus Tuple subtypes via a tuple-arg path.\nMesherCache(mesher) — preallocated scratch buffer; pass cache = … in hot loops to avoid the per-call index-map allocation.","title":"Type parameters"},{"category":"section","location":"#How-it-works","page":"Home","text":"Mesher(grid_size) precomputes the implicit binary tree of right triangles that subdivide the tile recursively. The tree has tile_size² × 2 − 2 nodes — one for every potential triangle at every level. Coordinates are packed into a flat Vector{UInt16} indexed by triangle id.\n\ncreate_tile(mesher, terrain) walks that tree bottom-up, computing the per-vertex maximum error: each non-leaf triangle's long-edge midpoint records the worst of its own approximation error and any descendant's error.\n\nget_mesh(tile; max_error) walks the tree top-down, emitting a triangle whenever the midpoint error is at or below max_error. Otherwise it recurses into the two children. Two passes — first count vertices and triangles for allocation, then fill the flat buffers.","title":"How it works"},{"category":"section","location":"#Numerical-parity-with-JS","page":"Home","text":"Internal arithmetic uses Float64 intermediates and Float32 storage, mirroring how JavaScript treats Float32Array reads as Number (F64) and only truncates on store. This yields byte-identical output to the reference @mapbox/martini library on the fuji.png fixture at every threshold tested.","title":"Numerical parity with JS"},{"category":"section","location":"#See-also","page":"Home","text":"Mesher, MesherCache, Tile, Mesh\ncreate_tile, get_mesh\nMapbox Martini — the original JavaScript\npymartini — Python port","title":"See also"}]
}
